import dashboard from './dashboard';
import chat from './chat';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, chat]
};

export default menuItems;