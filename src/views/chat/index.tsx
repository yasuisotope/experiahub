'use client';

import { Box, Typography } from '@mui/material';

const ChatPage = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h1">Chat Interface</Typography>
    </Box>
  );
};

export default ChatPage;